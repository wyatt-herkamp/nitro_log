{
  "root_loggers": [
    {"format": "[{{module({})}}] {{level({})}}: {{message({})}}",
      "structure_dump": true,
      "targets": [
        {
          "type": "console",
          "properties": {
            "format": "[{{module({})}}] {{level({})}}: {{message({})}}"
          }
        }
      ]
    }
  ]
}